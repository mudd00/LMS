# 🎯 GPS 기반 실시간 멀티플레이어 맵 구현 완료

## ✅ 구현 내용

지도 페이지(`/map-game`)에서 **GPS 기반으로 근처 플레이어의 캐릭터가 실시간으로 동기화**되어 화면에 보이도록 구현했습니다.

이는 Level1 맵의 실시간 동기화 로직과 동일한 방식입니다.

---

## 🏗️ 기술 구조

### 1️⃣ **프론트엔드 (React)**

#### MapGamePageV2.jsx
```javascript
// MultiplayerService 초기화
const multiplayerService = new MultiplayerService();
multiplayerService.connect(userId, username, false);

// 다른 플레이어의 위치 업데이트 수신
multiplayerService.onPositionUpdateCallbacks.push((data) => {
  setRemotePlayersData(...); // 상태 업데이트
});

// 1초마다 자신의 위치 브로드캐스트
setInterval(() => {
  multiplayerServiceRef.current.sendPositionUpdate(
    [x, y, z],
    heading,
    animation,
    modelPath
  );
}, 1000);
```

#### useMapGame.js 훅 확장
```javascript
// GameManager의 멀티플레이어 메서드 노출
const updateRemotePlayer = (userId, playerData) => {
  gameManagerRef.current.updateRemotePlayer(userId, playerData);
};

const removeRemotePlayer = (userId) => {
  gameManagerRef.current.removeRemotePlayer(userId);
};
```

### 2️⃣ **게임 엔진 (GameManager)**

#### GameManager.js 확장
```javascript
// 원격 플레이어 저장소
this.remotePlayers = new Map(); // Map<userId, { mesh, body, username, ... }>

// 원격 플레이어 업데이트
updateRemotePlayer(userId, playerData) {
  // 거리 제한: 100m 내에만 렌더링
  // 위치 보간: 부드러운 이동 (speed = 0.15)
  // 닉네임 표시: 3D Nameplate
}

// 원격 플레이어 제거
removeRemotePlayer(userId) {
  // 플레이어가 떠났을 때 3D 모델 삭제
}
```

### 3️⃣ **백엔드 (Spring Boot WebSocket)**

#### MultiplayerController.java
```java
@MessageMapping("/player.position")
@SendTo("/topic/positions")
public PlayerPositionDto updatePosition(PlayerPositionDto positionDto) {
  positionDto.setTimestamp(System.currentTimeMillis());
  return positionDto; // 모든 클라이언트에 브로드캐스트
}
```

#### PlayerPositionDto.java
```java
@Data
public class PlayerPositionDto {
  private String userId;
  private String username;
  private Double x, y, z;           // 3D 월드 좌표
  private Double rotationY;         // 바라보는 방향
  private String animation;         // idle/walk/run
  private String modelPath;        // 캐릭터 모델
  private Long timestamp;
}
```

---

## 📡 데이터 흐름

```
┌─────────────────────────────────────────────┐
│  플레이어 A가 GPS 위치 이동                 │
└──────────────┬──────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────┐
│  MapGamePageV2: sendPositionUpdate()        │
│  WebSocket -> /app/player.position          │
└──────────────┬──────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────┐
│  백엔드: MultiplayerController             │
│  /topic/positions로 브로드캐스트           │
└──────────────┬──────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────┐
│  플레이어 B: onPositionUpdateCallbacks     │
│  setRemotePlayersData() 호출                │
└──────────────┬──────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────┐
│  GameManager.updateRemotePlayer()           │
│  - 거리 확인 (100m 이내?)                  │
│  - 메시 생성 또는 위치 업데이트            │
│  - 위치 보간 애니메이션                    │
└──────────────┬──────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────┐
│  Three.js: 3D 캐릭터 렌더링                │
│  - 플레이어 B의 캐릭터가 보임              │
│  - 닉네임 Nameplate 표시                   │
└─────────────────────────────────────────────┘
```

---

## 🎮 실제 작동 방식

### 플레이어 A의 화면 (왼쪽: GPS 맵, 오른쪽: 3D 환경)
```
┌─────────────────┬──────────────────┐
│                 │                  │
│   GPS 맵        │   3D 환경        │
│  (플레이어 A)  │  (플레이어 A)   │
│   📍 위치표시    │   🧑 캐릭터      │
│                 │   🧑 플레이어 B  │
│                 │   (닉네임)      │
│                 │                  │
└─────────────────┴──────────────────┘
```

### 플레이어 B의 화면 (왼쪽: GPS 맵, 오른쪽: 3D 환경)
```
┌─────────────────┬──────────────────┐
│                 │                  │
│   GPS 맵        │   3D 환경        │
│  (플레이어 B)  │  (플레이어 B)   │
│   📍 위치표시    │   🧑 캐릭터      │
│                 │   🧑 플레이어 A  │
│                 │   (닉네임)      │
│                 │                  │
└─────────────────┴──────────────────┘
```

---

## 🚀 테스트 방법

### 준비물
- 백엔드 서버 실행: `./gradlew bootRun`
- 프론트엔드 서버 실행: `npm start`

### 테스트 시나리오

#### 시나리오 1: 단일 플레이어 테스트
1. 브라우저에서 `http://localhost:3000/map-game` 접속
2. 로그인 후 지도 페이지 진입
3. 콘솔 확인: "✅ WebSocket Connected"
4. 매초마다 위치 업데이트 로그 확인

#### 시나리오 2: 2명 이상 멀티플레이어 테스트
1. **플레이어 1**: 브라우저 1에서 `/map-game` 접속
2. **플레이어 2**: 브라우저 2에서 `/map-game` 접속 (다른 계정)
3. 플레이어 1의 콘솔에서 확인:
   ```
   🎮 Player joined: [플레이어 2 닉네임]
   ```
4. 플레이어 1의 우측 3D 화면:
   - 플레이어 2의 캐릭터가 표시됨
   - 플레이어 2가 이동하면 실시간으로 따라 이동
   - 닉네임이 캐릭터 위에 표시됨

#### 시나리오 3: 플레이어 이탈 테스트
1. 플레이어 2가 창을 닫음
2. 플레이어 1의 콘솔에서:
   ```
   👋 Player left: [플레이어 2 닉네임]
   ```
3. 플레이어 1의 3D 화면에서 플레이어 2의 캐릭터가 사라짐

---

## ⚙️ 주요 파라미터

### 위치 업데이트 빈도
**파일**: `src/pages/MapGamePageV2.jsx`
```javascript
setInterval(() => { ... }, 1000); // 1000ms = 1초
```
- 현재: 1초마다 위치 전송
- 네트워크 부하 vs 반응성 트레이드오프

### 렌더링 거리
**파일**: `src/features/map-game-v2/core/GameManager.js`
```javascript
this.remotePlayerDistance = 100; // 100m
```
- 100m 이상 떨어진 플레이어는 렌더링 안 함
- 성능 최적화

### 위치 보간 속도
**파일**: `src/features/map-game-v2/core/GameManager.js` (updateRemotePlayer)
```javascript
const speed = 0.15; // 0~1 사이의 값
```
- 0.15: 부드러운 이동
- 값이 클수록 빠른 이동
- 1.0: 즉시 이동

---

## 🔍 디버깅 팁

### 콘솔 로그 확인
```javascript
// 브라우저 개발자 도구 (F12) > Console

// 예상되는 로그:
✅ "WebSocket Connected"
✅ "🎮 Player joined: [닉네임]"
✅ "Player position update" (매초)
✅ "👋 Player left: [닉네임]"
```

### Network 탭 확인
1. F12 > Network 탭
2. "WS" 필터로 WebSocket만 표시
3. `ws://localhost:8080/ws` 연결 확인
4. Messages 탭에서:
   - Sent: `/app/player.position` (1초마다)
   - Received: `/topic/positions` (다른 플레이어 위치)

### 3D 화면 확인
- 좌측 GPS 맵: 자신의 위치
- 우측 3D 환경: 캐릭터와 다른 플레이어 표시

---

## 🐛 문제 해결

### 문제 1: 다른 플레이어가 안 보임
**원인**: 
- 같은 계정으로 로그인
- 100m 이상 떨어져 있음
- WebSocket 연결 끊김

**해결**:
- 다른 계정 사용
- 거리 내에서 테스트
- 콘솔에서 "WebSocket Connected" 확인

### 문제 2: 캐릭터가 부자연스럽게 움직임
**원인**:
- 네트워크 지연
- 위치 보간 값이 너무 큼/작음

**해결**:
- 위치 보간 speed 값 조정 (0.1 ~ 0.3)
- 네트워크 안정성 확인

### 문제 3: WebSocket 연결 안 됨
**원인**:
- 백엔드 서버 미실행
- localStorage에 userId/username 없음

**해결**:
- 백엔드 서버 실행 확인: `./gradlew bootRun`
- 로그인 후 localStorage 확인

---

## 📊 성능 지표

### 목표
- 위치 업데이트: 1초마다
- 렌더링 거리: 100m 내
- FPS: 60FPS 목표
- 메모리: 플레이어당 ~5MB

### 측정
```javascript
// MapGamePageV2.jsx에서 확인
const perfMetrics = getPerformanceMetrics();
console.log('FPS:', perfMetrics.fps);
console.log('Memory:', perfMetrics.memory);
```

---

## 🚀 향후 개선

### Phase 1 (즉시)
- [ ] 백엔드에 `player_locations` 테이블 생성
- [ ] 서버 사이드 거리 필터링 구현
- [ ] 연결 끊김 자동 정리

### Phase 2 (단기)
- [ ] 플레이어 목록 UI 표시
- [ ] 플레이어 상태 (idle/walk/run) 동기화
- [ ] 플레이어 아바타 모델 동기화

### Phase 3 (중기)
- [ ] 플레이어 간 인터랙션 (친구추가, 미니게임)
- [ ] 플레이어 검색 기능
- [ ] 갱신 내역 저장

---

## 📝 코드 참고

### MultiplayerService 사용법
```javascript
// 연결
const service = new MultiplayerService();
service.connect(userId, username, false);

// 위치 업데이트 리스너
service.onPositionUpdateCallbacks.push((data) => {
  console.log('Player position:', data.x, data.y, data.z);
});

// 위치 전송
service.sendPositionUpdate(
  [x, y, z],           // 위치
  heading,             // 바라보는 각도
  'walk',              // 애니메이션
  modelPath,           // 모델 경로
  false                // 아바타 변경 중?
);

// 연결 해제
service.disconnect();
```

### GameManager 사용법
```javascript
// 원격 플레이어 업데이트
gameManager.updateRemotePlayer(userId, {
  username: 'Player2',
  position: [100, 5, 50],
  heading: Math.PI / 4,
  animation: 'walk',
  modelPath: '/path/to/model.gltf'
});

// 원격 플레이어 제거
gameManager.removeRemotePlayer(userId);
```

---

## 📞 지원

문제가 발생하면 다음을 확인하세요:

1. **콘솔 로그** (F12)
2. **Network 탭** (WS 필터)
3. **백엔드 로그** (터미널)
4. **주요 파일 수정 사항**:
   - `src/pages/MapGamePageV2.jsx` ✅
   - `src/features/map-game-v2/core/GameManager.js` ✅
   - `src/features/map-game-v2/hooks/useMapGame.js` ✅

---

**구현 완료**: 2025-12-31
**테스트 상태**: 컴파일 ✅, 구조 ✅, 실행 대기 중
